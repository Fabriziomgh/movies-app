import { View, Text } from 'react-native';
import React from 'react';

const saved = () => {
   return (
      <View className="">
         <Text>saved</Text>
      </View>
   );
};

export default saved;
